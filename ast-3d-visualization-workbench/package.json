{
  "name": "ast-3d-visualization-workbench",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter ./apps/* run dev",
    "dev:backend": "pnpm --filter @ast-viz/backend run dev",
    "dev:frontend": "pnpm --filter @ast-viz/frontend run dev",
    "build": "pnpm --filter @ast-viz/shared run build && pnpm --parallel --filter ./apps/* run build",
    "build:backend": "pnpm --filter @ast-viz/shared run build && pnpm --filter @ast-viz/backend run build",
    "build:frontend": "pnpm --filter @ast-viz/shared run build && pnpm --filter @ast-viz/frontend run build"
  }
}
